#include <iostream>
#include <string>
#include <algorithm>
using namespace std;


class Solution {
public:
    bool isMatch(string s, string p) {
        int n = int(s.length());
        int m = int(p.length());
        bool dp[n+1][m+1];


    }
};

int main(){
    string s = "aa";
    string p = "a";
    class Solution solution;
    cout << solution.isMatch(s, p) << endl;
    return 0;
}
